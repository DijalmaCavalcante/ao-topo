import { css } from '@bake-js/-o-id/dom'

function style () {
  return css`
    .musicname {
      display: flex;
      flex-direction: column;
      font-family: 'maven pro';
    }
`
}

export default style
